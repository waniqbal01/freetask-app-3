# NOTE: Jangan commit env sebenar. Fail ini hanya contoh konfigurasi.
DATABASE_URL=postgresql://postgres:password@localhost:5432/freetask?schema=public
JWT_SECRET=changeme_jwt_secret
JWT_EXPIRES_IN=7d
PORT=4000
# Tambah URL frontend anda jika perlu (contoh: http://localhost:PORT atau domain staging)
# Tambah origin lain jika test dari iOS simulator / domain production
# Contoh: https://app.freetask.my
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4000,http://localhost:5173,http://127.0.0.1:5173
